fastapi==0.115.0
uvicorn[standard]==0.32.0
anthropic==0.40.0
# Used as a generic OpenAI-compatible client for NVIDIA NIM and the
# OpenRouter fallback — see app/agents/base.py.
openai==2.44.0
# chromadb 0.4.24 still uses np.float_, which NumPy 2.0 removed. Must stay
# on the 1.x line or the app crashes at import with AttributeError.
numpy==1.26.4
# Embeddings come from chromadb's bundled ONNX MiniLM (see app/rag/embeddings.py).
# torch + sentence-transformers were removed deliberately: they added ~300MB of
# resident memory for a 22M-param model and blew the 512MB container limit.
chromadb==0.4.24
pydantic==2.5.3
pydantic-settings==2.1.0
httpx==0.27.2
python-multipart==0.0.12
python-dotenv==1.0.1
